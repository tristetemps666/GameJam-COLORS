using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class shootBallon : MonoBehaviour
{
    /*public GameObject player;
    private Vector3 différence;
    void OnCollisionEnter(Collision collision){
        if (collision.gameObject.name == player.name){
            // c'est ici qu'on ajoute une force à notre ballon !
        }
    }
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void FixedUpdate()
    {
        différence = (transform.position - player.Transform.position).normalized; // on récup le vecteur directeur sur lequel on va faire l'envoie
        différence.y = 0f; // on récup que la direction dans le plan
    }*/
}
